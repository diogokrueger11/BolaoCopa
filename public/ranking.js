import{setupNav,toast,avatar}from"./shared.js";setupNav("ranking");
fetch("/api/ranking").then(res=>res.json()).then(data=>{document.querySelector("#ranking-summary").textContent=`${data.finishedGames} jogos contabilizados`;document.querySelector("#ranking-body").innerHTML=data.ranking.length?data.ranking.map(row=>`<tr><td><b>${row.position}</b></td><td><div class="person">${avatar(row.profile)}<span><b>${row.profile.name||"Participante"}</b></span></div></td><td>${row.correct}</td><td><strong>${row.points}</strong></td></tr>`).join(""):'<tr><td colspan="4" class="empty">Nenhum participante salvou palpites ainda.</td></tr>'}).catch(()=>toast("Não foi possível carregar o ranking."));
